<%- include("./partials/head.ejs") %>

<div id="banner">
    <h1 id="slogan">WHERE PEOPLE MEET</h1>

    <!-- Buttons -->
    <div id="banner-buttons">
        <% if (!user) { %>
            <a href="/signup" class="banner-button" id="sign-up">Sign Up</a>
            <a href="/login" class="banner-button" id="login">Login</a>
        <% } %>
        <a href="#" class="banner-button">Top Events</a>
    </div>
</div>

<%- include('./partials/footer'); -%>