<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EventsHub | </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous" defer>
    <link rel="stylesheet" href="/styles.css" type="text/css" defer>
</head>
<body>
    <nav id="navbar">
        <a href="/"><img src="/logo.png" alt="EventsHub Logo" id="logo"></a>
        <a href="/events"><h3 class="nav-text">Events</h3></a>
        <a href="/businesses"><h3 class="nav-text">Businesses</h3></a>
        <a href="/forum"><h3 class="nav-text">Forum</h3></a>
        <div id="nav-right">
            <form action="/search" method="GET">
                <input type="text" name="q" placeholder="Search...">
                <button type="submit" id="searchbar">Search</button>
            </form>
            <% if (!user) { %>
                <!-- If user is not logged in -->
                <a href="/signup" class="nav-link">Sign Up</a>
                <a href="/login" class="nav-link">Login</a>
            <% } else { %>
                <!-- If user is logged in -->
                <a href="/logout" class="nav-link">Sign Out</a>
                <a href="/profile/<%= user._id %>" class="nav-link">
                    <img src="<%= user.profileImageUrl || '/default-profile-image.jpg' %>" alt="Profile Picture" id="profile-pic">
                </a>
            <% } %>
        </div>
    </nav>
    
    <script>
        fetch ("https://jsonplaceholder.typicode.com/users")
            .then(res => res.json())
            .then(data => {})
    </script>